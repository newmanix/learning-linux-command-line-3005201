{
  "extensions": [
    "GitHub.github-vscode-theme"
  ],
  "onCreateCommand": "cp -f .bashrc ~/.bashrc && echo 'if [ -f ~/.bashrc ]; then . ~/.bashrc; fi' > ~/.bash_profile && . ~/.bashrc",
  "postAttachCommand": "cp -f .bashrc ~/.bashrc && . ~/.bashrc && git pull --all"
}
