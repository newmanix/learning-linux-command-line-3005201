{
    "extensions": [
      "GitHub.github-vscode-theme"
    ],
    "onCreateCommand": "echo 'if [ -f ~/.bashrc ]; then source ~/.bashrc; fi' >> ~/.bash_profile && source ~/.bashrc",
    "postAttachCommand": "git pull --all"
}
