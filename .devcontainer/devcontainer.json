{
    "extensions": [
      "GitHub.github-vscode-theme"
    ],
    "onCreateCommand": "cp .bashrc ~/.bashrc && echo 'if [ -f ~/.bashrc ]; then . ~/.bashrc; fi' >> ~/.bash_profile && . ~/.bashrc",
    "postAttachCommand": "git pull --all"
}
